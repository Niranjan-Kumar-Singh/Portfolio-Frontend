/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"], // Add paths where Tailwind should look for class names
  theme: {
    extend: {}, // Extend styles here if needed
  },
  plugins: [],
};